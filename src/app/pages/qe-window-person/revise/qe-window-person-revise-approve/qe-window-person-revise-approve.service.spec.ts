/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { QeWindowPersonReviseApproveService } from './qe-window-person-revise-approve.service';

describe('Service: QeWindowPersonReviseApprove', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [QeWindowPersonReviseApproveService]
    });
  });

  it('should ...', inject([QeWindowPersonReviseApproveService], (service: QeWindowPersonReviseApproveService) => {
    expect(service).toBeTruthy();
  }));
});
